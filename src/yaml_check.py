# Verify the YAML config to ensure that it's valid / there isn't a duplicate file extensions or anything.

# Load yaml file

# parse it. Check for dupe values

# If there is anything - log it into a log file.